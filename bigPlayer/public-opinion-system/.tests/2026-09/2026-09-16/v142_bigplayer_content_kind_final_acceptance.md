# v142 BigPlayer 内容类型旧口径验收（已作废）

> 作废状态：2026-09-16，用户已弹窗拍板的新规则已覆盖本报告的分类口径。本报告按“标题是否为空”判定帖子/动态，**不得纳入发版依据**。
>
> 后续验收口径：以源站顶层 `type=0` 判定帖子、顶层 `type=1` 判定动态，并重新覆盖对应内容展示、筛选及其他社区隔离。

- 验收时间：2026-09-16
- 环境：`https://lfy3001.dev.q1op.com`（免登录实例）
- 范围：BigPlayer 社区抓取内容的“帖子 / 动态”展示与筛选。
- 原结论：通过（仅旧口径）；当前状态：作废。

## 真实样本

| 区域 / 社区 | external_id | 原始标题 | 页面验证结果 |
| --- | --- | --- | --- |
| 境外 / Last Light | `1487612` | `Dudhsagar fall 😍` | 列表显示标题及类型“帖子”；选择“帖子”后仍可见，选择“动态”后列表中不可见。 |
| 境内 / 超能世界国服版 | `918470` | `null` | 列表类型为“动态”；列表与详情均不显示“（无标题）”。 |

## 补充验证

- 页面控制台未发现来自 `lfy3001.dev.q1op.com` 的 `error` 或 `warn`。
- 真实库目前没有 `title=''` 和纯空白标题样本；该两项为数据覆盖缺口，不伪造数据，不阻塞本次结论。

## 非阻塞观察

对境外样本选择“动态”后，列表正确显示“暂无匹配内容”，但分页文案仍显示筛选前的“共 1 条”。当前类型筛选为已加载页的客户端过滤，不将该分页数作为筛选后端总数断言；建议后续统一筛选后的统计口径。
