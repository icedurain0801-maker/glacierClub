# v143 采集源删除验收

- 验收时间：2026-09-16
- 环境：`https://lfy3001.dev.q1op.com`（免登录实例）
- 范围：采集源运行配置删除；历史内容、分析与告警须保留。
- 状态：通过。

## 已验证

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 前端确认文案 | 通过（定向测试） | `是否删除 ${name} 采集源？`，且确认前即取消则直接返回。 |
| 取消零请求 | 通过（定向测试） | `source-delete-ui.test.js`：2/2 通过，确认取消分支在 `DELETE` 前返回。 |
| 成功 / 失败提示 | 通过（定向测试） | 删除成功提示“采集源已删除，历史内容已保留”；失败展示服务端错误。 |
| running 任务保护 | 通过（开发侧定向测试结果） | `DELETE /sources/:id` 在运行任务存在时返回 `409 SOURCE_RUN_ACTIVE`，不发生半删除。 |
| 历史数据保留 | 通过（开发侧定向测试结果） | 验证凭据、检查点、能力、调度和 queued 任务清理；历史内容、分析、告警关联保留。 |

## 初始浏览器通道情况（已解除）

1. 已切换至境内“SDK自动化测试”社区，该社区开始时无采集源。
2. 已仅创建专用 fixture：`E2E删除验收Fixture-20260916`，`source_id=5b31d570-91e5-4c44-96a7-2f47446938b3`，平台为 `bigplayer_h5`、社区为 `8b1f0000000000000000000000000021`（SDK自动化测试）；核对时 `enabled=1`、`deleted=null`。未操作任何既有真实采集源。
3. 点击该 fixture 的“删除”按钮时，浏览器自动化通道超时；未出现可读取的确认框，未发送 DELETE 请求，fixture 仍在。
4. 初始通道无法完成页面验收；后续已由支持原生 `confirm` 的浏览器通道补齐，最终结果见下节。

## 后续动作

已由支持原生 `confirm` 的浏览器通道完成上述 fixture 删除，结果如下：

1. 确认文案严格为“是否删除 E2E删除验收Fixture-20260916 采集源？”。
2. 确认后强制刷新 `lfy3001` 页面，显示“共 0 个账号源 / 暂无符合条件的采集源”。
3. `source_id=5b31d570-91e5-4c44-96a7-2f47446938b3` 在 localhost `4320` sources API 与 `lfy3001` 同源 API 均为 `match=0`。
4. 浏览器控制台 `error` / `warn` 为空。

结合前端定向测试 2/2（精确确认文案、取消零 DELETE、成功/失败提示）与后端定向测试 3/3（关联运行配置清理、running 返回 409、历史内容/分析/告警保留），本项最终验收通过。
