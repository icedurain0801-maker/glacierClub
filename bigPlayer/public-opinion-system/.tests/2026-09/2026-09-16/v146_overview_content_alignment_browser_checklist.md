# v146 四卡同源与负面热帖深链真实浏览器验收清单

- 状态：待开发交付。
- 门禁：仅真实浏览器 PASS 与本报告最终路径可关闭；不以 Mock、静态测试或 API 200 替代。

| 编号 | 验收项 | 通过标准 |
| --- | --- | --- |
| 1 | 四卡布局 | 四卡均显示，待人工验证卡不回归；桌面与 `375x812` 无空占位、裁切或横向溢出。 |
| 2 | 内容同源 | 同 Scope、精确时间范围下，发布内容卡和负面内容卡数值分别等于内容页对应 `total`。 |
| 3 | 热帖集合 | 每条负面热帖 ID 均属于同一负面内容集合，顺序保持既有排序规则。 |
| 4 | 全部内容深链 | 跳转携带完整 Scope、时间范围、`negative` 筛选；清除 `page`、`contentId` 等旧定位参数。 |
| 5 | 导航恢复 | 深链后刷新、后退、前进均恢复相同筛选与数据。 |
| 6 | 稳定性 | 控制台无 `error/warn`，相关页面 API 无异常或无效 Scope 请求。 |
