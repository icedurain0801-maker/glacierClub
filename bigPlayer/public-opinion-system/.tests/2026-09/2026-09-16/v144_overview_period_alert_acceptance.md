# v144 概览时间窗口与当前告警验收

- 验收时间：2026-09-16
- 环境：`https://lfy3001.dev.q1op.com` 免登录实例
- 范围：今日 / 近7天 / 近30天统一内容时间窗口、当前告警口径、指标卡改造。
- 结论：通过。此前外网 API 参数不兼容和移动视口布局问题均已复测修复。

## 真实浏览器结果

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 今日 | 通过 | 页面正常展示“今日概览”、今日发布内容 `105`、负面内容 `13`、当前告警 `0`；告警区为空态。 |
| 近7天 | 通过 | URL 为 `period=7d`；稳定回填后显示“近7天发布内容” `1,133`、负面内容 `168`、当前告警 `0`，趋势和内容区文案同步为“近7天”。 |
| 近30天 | 通过 | URL 为 `period=30d`；显示“近30天发布内容” `1,831`、负面内容 `286`、当前告警 `1`；当前告警列表恰有 1 条待处理告警，卡片与列表同数。 |
| 快速切换 | 通过 | 按 `today → 7d → 30d` 连续切换后，最终稳定呈现近30天数据，无加载失败或白屏。 |
| 三卡及移除项 | 通过 | `#metrics` 实际有 3 个子项；页面未出现“紧急内容”或“待人工验证”。桌面三卡无空占位且自动补位。 |
| 移动布局 | 通过 | 复测 `375x812`：`.admin-main` 为 `left=0`、`top=220`、`width=360`；`document/body.scrollWidth=360`，小于 viewport `375`，无页面级横向溢出，三张指标卡均可见。 |
| 桌面布局 | 通过 | 复测 `1440x900`：主区从 `left=220` 开始，三张指标卡同一行；滚动条占用后的可用主区宽度为 `1205`，无卡片重叠或空占位。 |
| 控制台 | 通过 | `lfy3001.dev.q1op.com` 页面 `error` / `warn` 为空。 |

## 口径与阻塞

本次新建标签强刷后，真实部署的 `today`、`7d`、`30d` 均正常加载，旧 API 参数不兼容不再复现。已完成的真实页面证据包括卡片、趋势和内容区范围文案同步，以及近30天当前告警卡片与列表同数。

- `pending` / `processing`、按 `alert_id` 去重、`resolved` / `false_positive` 排除、关联内容 `published_at` 北京时间 `[from,to)` 半开区间和边界时间，已由开发侧两项概览路由集成测试与仓储查询契约覆盖。
- 页面真实范围内的告警样本为近30天 1 条待处理告警；无法在不改写数据的前提下构造全部状态组合。

开发侧补件：概览定向 2/2、删除软删定向 1/1、`server npm test` 404/404 通过，0 失败。

## 缺陷退回

此前 `P1` 移动布局缺陷已修复并通过复测，无待退回开发的概览问题。
