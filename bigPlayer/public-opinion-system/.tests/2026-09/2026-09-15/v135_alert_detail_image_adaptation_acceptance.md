---
date: 2026-09-15
status: blocked
scope: 告警详情抽屉“原文内容”图片自适应回归
owner: 测试负责人
---

# 测试结论

结论：阻塞，不能判定通过，退回开发负责人补充可验收样本/环境后再回归。

## 测试环境

- 页面：`https://lfy3001.dev.q1op.com/admin/PublicOpinion/alerts.html?regionCode=domestic&communityId=00000000-0000-0000-0000-000000000101&platform=bigplayer_h5&tab=risk`
- 标签：新建浏览器标签，标签页 ID `3`
- 筛选：境内 / 超能世界国服版 / BigPlayer社区 / 风险告警管理
- 告警：`抱怨活动存在付费压力`，发现时间 `2026-09-10 17:34:48`
- 当前视口：`1280 x 720`

## 已验证

1. 详情抽屉可打开，原文区域可见图片。
2. 两张有效图片的渲染宽度均为 `474.984px`，不超过目标上限 `480px`。
3. 详情容器 `clientWidth=501px`、`scrollWidth=501px`，当前宽视口无横向溢出；页面 `body.scrollWidth=1280px`。
4. 有效图片原始尺寸均为 `1260 x 2800`，渲染比例约 `0.4510`，原始比例 `0.45`，比例保持正常。
5. 截图已留存于本次验收过程：详情抽屉右侧可见图片未突破内容边界。

## 未验证/阻塞

1. 未能切换到窄抽屉/窄视口，故“窄抽屉下不溢出”尚无页面证据。
2. 当前样本没有可用的小尺寸图片：第三个图片节点 `naturalWidth=0`、`naturalHeight=0`，渲染为约 `2 x 2px` 的失败占位，不能作为“小图保持自然尺寸”的有效样本。
3. 因上述两项，不能确认 CSS 规则在窄容器和真实小图场景下完整满足验收要求。

## 退回开发负责人

请提供：

- 可在测试环境稳定加载的真实小尺寸原文图片样本；
- 可调整到窄抽屉宽度的验收入口，或提供对应真机/窄视口页面证据；
- 修复/补充后通知测试负责人重新回归。
