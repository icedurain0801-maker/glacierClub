# v206 BigPlayer H5 站点地址表单真实浏览器验收

- 结论：**FAIL / P0 阻断。** 当前验收页面未加载多站点 textarea，无法验证宽度修复和多站点交互。
- 范围：仅管理页前端表单；未保存、未授权、未检测能力、未启动同步或真实抓取。

## 实测

1. 打开境内「超能世界国服版」→「抓取账号管理」→ BigPlayer H5 来源「管理」。
2. 详情加载后的“大玩家 H5 站点”区域仅显示单行文本框 `cfgBaseUrl`，当前值为存量链接。
3. 页面未渲染多站点 textarea `cfgSiteUrls`，也没有站点行添加/删除控件。

## 判定

- 不能执行“一次点击获得焦点，输入 3 条合法 URL”。
- 不能执行添加/删除行，不能查看非法 URL 或重复 URL 校验。
- 不存在目标 textarea，故桌面与窄屏宽度坍缩/溢出用例均无法成立。
- 工作区的 `admin/PublicOpinion/assets/sources.js` 已包含 `cfgSiteUrls` 相关代码，但真实浏览器仍加载单行 `cfgBaseUrl` 表单；当前部署/静态资产与待验收实现不一致。

## 退修要求

开发负责人须先将包含多站点表单及宽度修复的前端资产部署至当前验收域名，提供可识别构建版本；不修改业务数据、不启动同步。测试负责人随后复验：桌面/窄屏布局、三链接输入、行增删、非法/重复可见校验。通过后才恢复 v205 多站点真实三链接准入验收。
