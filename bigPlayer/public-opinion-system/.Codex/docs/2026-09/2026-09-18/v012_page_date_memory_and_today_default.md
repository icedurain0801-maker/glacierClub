# v012 页面时间记忆与默认今日

- 范围：抓取内容管理与舆情概览前端时间筛选。
- 内容页按有效 URL 时间范围、内容页 localStorage、今日的顺序恢复；无效 URL 不采用并回退至今日；点击“清空”会移除该页时间记忆。
- 概览页按有效 URL `period`、概览页 localStorage、今日的顺序恢复；页面各自使用独立 key。
- 恢复后的既有初始化继续调用 `loadStats()` / `load()`，实际请求仍携带已恢复的时间范围或 period。
- 未涉及采集、授权、P0 补跑、翻译、部署或后端写入。
