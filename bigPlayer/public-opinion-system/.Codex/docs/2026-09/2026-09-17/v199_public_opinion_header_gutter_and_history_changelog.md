# v199 PublicOpinion 顶部留白与历史作用域修复

日期：2026-09-17

## 变更内容

- `content.html` 的标题区改用统计卡片既有 gutter：桌面 `28px`、窄屏 `16px`；顶部和首个内容区保持呼吸间距，不再贴边或外扩。
- 六页标题区均复用相同的外层间距与内容栅格边界，且 CSS 缓存版本升级为 `public-opinion.css?v=199`。
- `scope.js` 在浏览器前进/后退时保留历史 URL 快照，不再以异步归一化结果覆盖其中的 `communityId`。
- 告警页和运行记录页在历史恢复期间跳过自身 URL 重写；运行记录页恢复分页和展开记录状态。
- 六页作用域脚本版本升级为 `scope.js?v=147`；告警和运行记录脚本分别升级为 `v=146`。

## 验证

- 本地浏览器逐页桌面与 375px 验证：标题区四周留白、与首个内容区左右对齐、无横向溢出。
- 控制台无 error/warn；`node --check` 验证 scope、alerts、collection-runs 脚本。
- 本机无可用社区，无法真实完成境内/境外选择后的历史回退；需在外网有效社区数据下回归该路径。
