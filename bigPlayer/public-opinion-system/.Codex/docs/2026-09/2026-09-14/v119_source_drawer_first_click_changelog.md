# v119 采集源管理抽屉首击加载体验

## 变更

- 管理按钮首击立即打开抽屉并展示加载态，详情接口异步完成后回填原有配置与授权内容。
- 详情请求失败时在抽屉内展示错误信息和“重试”入口；重试会强制重新发起详情请求。
- 同一采集源在抽屉已打开且正在处理时，重复点击管理按钮不会重复请求。
- 未修改同步、授权、频率、抓取数据逻辑及“开始同步”按钮行为。

## 验证

- `node --test bigPlayer/admin/PublicOpinion/assets/source-status.test.js`：14/14 通过。
- `git diff --check`：未发现本次变更新增的空白错误（仅有工作树既有换行格式提示）。
- 已尝试通过内置浏览器访问本地页面；浏览器策略拒绝 `localhost/127.0.0.1`，且现有 `3000` 端口为 API 服务并返回静态页面 404，因此未能完成真实浏览器点击验收。加载态、慢响应回填、失败重试和重复点击均由测试 harness 覆盖。
