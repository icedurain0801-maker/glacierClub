# Q1 分片抓取与北京时间窗口补充

日期：2026-08-26

## 变更内容

- 保留 Q1 每日任务按北京时间前一天计算的半开窗口 `[00:00, 次日 00:00)`。
- 保留有界窗口按固定时长切分、分片独立分页及重叠范围去重逻辑。
- 抓取 summary 增加 raw 帖子数、最终唯一帖子数和实际分片总数；`posts` 继续表示最终唯一帖子数以兼容现有调用方。
- 日报增加 raw/unique 帖子、评论与回复、数据库导入总量、未变更量、失败批次和失败条目等明确统计字段。
- `analysisEligibleIds` 继续作为本批次分析范围的唯一来源，并保持 collection partial 的分析门禁。
- 前端内容管理日期筛选统一使用 `Asia/Shanghai`，结束时间为次日零点，形成左闭右开范围。

## 验证范围

- Python：时间分片边界、重叠帖子去重、导入批次统计及现有导入安全测试。
- Node：日报统计字段、固定北京时间窗口、部分导入继续分析、采集不完整阻断和敏感信息脱敏。
- 前端：北京时间跨 UTC 午夜、日期进位、查询范围起止参数。

未执行真实 Q1 抓取、导入、AI 分析或计划任务重跑。
