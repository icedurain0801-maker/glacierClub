# v002 变更记录

日期：2026-09-16

## 概览统一时间筛选与当前告警口径

- 时间范围统一为“今日 / 近7天 / 近30天”。服务端按北京时间生成并使用 `published_at` 半开区间 `[from,to)`。
- 概览指标、趋势、负面率、负面热帖、议题分布与当前告警均使用同一内容时间窗口。
- 当前告警仅包含 `pending`、`processing`，按关联内容的 `published_at` 过滤；多内容关联以 `alert_id` 去重，`resolved`、`false_positive` 与无关联内容不计入。
- 移除“紧急内容”独立统计，改为复用当前告警列表的 `activeAlertCount`；卡片数与列表数一致，负面内容保持独立。
- 移除“待人工验证”指标卡；桌面端指标网格改为自动补位，保留既有响应式断点。
- 时间范围切换时，概览、趋势与处置、内容与议题的标题文案同步更新；已有请求序列号继续阻止旧响应覆盖新结果。

## 验证

- `node --test --test-concurrency=1 --test-name-pattern="GET /overview" test/app.routes.test.js`：2 项通过。
- `node --test --test-name-pattern="getOverview 返回完整归属" test/repository.test.js`：1 项通过。
- JS 语法检查与前端结构检查通过；浏览器只读验证“近7天”切换后相关标题文案同步更新。
- 删除采集源集成测试的 fixture 清理补齐 `po_analyses`：仅清理 `alert-detail-%` / `quality-test-%` 测试 ID 前缀，避免失败中断后留下孤立分析记录，未改变采集或删除业务逻辑。
- 概览页在 560px 以下恢复纵向移动布局，侧栏不再挤占主内容的横向空间，页面级横向溢出被裁剪；概览卡片与三段内容保持既有断点的换行规则。
- 补充回归：概览路由定向 2/2、删除软删定向 1/1、服务端 `npm test` 404/404 通过，0 失败。

## 本机运行版本恢复

- 外网验收发现 4320 API 仍运行旧内存版本，拒绝 `7d` / `30d`；工作树契约已正确为 `today` / `7d` / `30d`。
- 仅重启本机 4320 API，使其加载当前 `server/src/app.js`；未修改数据库、Worker、3001 代理或远程生产服务。
- 4320、localhost 3001、LAN 3001、外网三档概览 API 均返回 HTTP 200 和真实数据结构；三层页面、CSS、JS 与告警 API 均返回 HTTP 200。
- 已重新流转测试负责人进行外网强刷浏览器验收；最终结论待测试负责人确认。不 push、不发版。
