# v001 变更记录

日期：2026-09-16

## 抓取内容管理：BigPlayer 帖子与动态

- 仅在 `bigplayer_h5` 范围内，将有真实标题的帖子展示为“帖子”，将 `null`、空字符串和纯空白标题的帖子展示为“动态”。
- 动态以正文作为列表主文本，不再展示“（无标题）”；详情抽屉标题显示“动态”。
- 在“全部分析等级”右侧增加“全部类型 / 帖子 / 动态”本地筛选；切换到 TapTap、Discord 等平台时自动隐藏并清空该筛选。
- 类型筛选不会修改内容、统计、进度、采集或调度接口参数。筛选作用于当前已加载页，因此结果提示为“当前页 N 条（已按类型筛选）”。

## 验证

- `node --check admin/PublicOpinion/assets/content.js`
- Node 定向规则校验：标题、`null`、空字符串、纯空白及非 BigPlayer 平台共 7 项全部通过。

## 抓取账号管理：删除采集源

- 列表操作栏新增“删除”，确认文案为“是否删除 XXX 采集源？”，取消确认不发起删除请求。
- 删除在单个数据库事务中执行；存在运行中任务时返回 `409 SOURCE_RUN_ACTIVE`，不会产生半删除。
- 删除后采集源保留软删除引用，账号运行配置失效；凭据、检查点、能力、调度状态被清理，排队任务终止。
- 已抓取的帖子、评论、AI 分析及告警关联全部保留，可继续在历史页面查询。

## 删除功能验证

- `node --test --test-name-pattern="DELETE /sources/:id" test/app.routes.test.js`：3 项通过。
- `node --test .tests/2026-09/2026-09-16/source-delete-ui.test.js`：2 项通过。
- `node --check` 与 `git diff --check` 通过。
- `lfy3001.dev.q1op.com` 真实浏览器验证：删除按钮可见；确认文案正确；取消后列表仍为 1 个采集源，未删除真实数据。
