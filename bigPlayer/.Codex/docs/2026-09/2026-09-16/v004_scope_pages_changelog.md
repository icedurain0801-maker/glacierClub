# v004 六页 Scope 接入变更记录

日期：2026-09-16
Status: ready_for_qa

- 概览 `assets/app.js`：沿用员工的生命周期、同 Scope 请求快照、无社区空态和序列保护。
- 内容 `assets/content.js`：保留列表/统计接入，补详情读取的 Scope 取消保护，切换关闭旧详情并刷新候选、统计、列表。
- 通知 `assets/alerts.js`：风险、优质内容及来源候选读取统一 Scope；添加列表序列保护、无社区空态，详情取消不回填旧内容。
- `scope.js` 仅增加请求适配器 `withScope`，保留已通过状态核心；适配各页原有 API 响应格式，注入地区/社区并登记取消/释放。
- 内容和通知的 `node --check`、按文件 `git diff --check` 通过。
- 账号 `assets/sources.js`：来源列表、详情、同步进度恢复的请求自动带 Scope；切换关闭旧详情和进度，校验刷新 epoch，无社区显示空状态。语法和按文件空白检查通过。
- 任务记录 `assets/collection-runs.js`：列表、候选、运行详情与展开分页读取统一 Scope；取消保留 AbortError；切换清旧展开/来源/运行 ID 并刷新，无社区不请求。语法和按文件空白检查通过。
- 关键词 `assets/keywords.js`：规则读取/保存显式地区社区，保留平台局部替换合同；读取取消不报错误，无社区显示明确空态并禁止保存。语法和按文件空白检查通过。
- 六页页面资源与共享脚本 cachebuster 统一为 `145`。
- 六页移动容器最小约束仅注入舆情页面，解决原有横向 flex 挤压；375x812 六页主区宽度均为375，无页面级横向溢出，桌面保持原样。
- Scope 行为测试 4/4；localhost 和外网真实浏览器测试各 2/2（六页继承、地区联动、历史恢复、API参数、Console及无社区禁请求）；服务端 `npm --prefix server test` 404/404。
- 开发负责人最小集成审核通过，待测试负责人独立真实浏览器结论。不 push、不发版。

## 真实边界场景补件

- 真实目录只读核对：domestic 23项/20启用，overseas 21项/20启用，现有免登录权限范围不存在合法零启用社区地区。
- 增加真实接口网络失败防护：初始化仍建立不可用Scope，禁用社区/平台，页面按钮不发缺Scope业务请求；失败不覆盖最近session选择。
- 修复空URL社区偏好匹配候选空字段的问题，保证非首项合法session选择不被默认项覆盖；行为测试5/5。
- 新增 `v145_scope_real_edge_browser.test.js`：CDP阻断真实社区网络请求，六页业务请求0；真实接口响应原样延迟透传，六页均乱序[2,1]并保持最新Scope，记录真实body SHA-256；2/2通过，不伪造payload、不写真实数据。
- 网络失败不可用分支不等价于后端返回零社区集合，确切环境限制已交测试负责人和项目经理确认覆盖边界。
