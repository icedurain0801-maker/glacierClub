# v006 变更记录

- 调整 `client/domestic/home/WelfareTask.html` 的页面壳布局：桌面端在固定 C 端侧边栏右侧可用区域内水平居中。
- 将福利任务底部导航移入 `375px` 内容壳，确保桌面端底部栏与 C 端页面等宽；窄屏下内容与底部栏均为 `100vw`。
- 将福利助手入口改为内容壳内定位，避免随右侧工作区宽度变化而错位。
- 更新 C 端页面脚手架 skill，明确内容壳居中和底部导航等宽规范。
