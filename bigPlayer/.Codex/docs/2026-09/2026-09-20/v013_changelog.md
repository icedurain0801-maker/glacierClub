# v013 变更记录

- 首页发帖浮层采用已确认的「标题 + 总浏览数」话题方案：四个等高、可勾选的英文推荐话题，标题最多展示两行。
- 推荐话题右上角提示更新为「勾选后发布时自动带入」。
- 点击「攻略 / 帖子」或「发心情」后，将所选话题通过受控参数带入对应的国内发布页；发布页仅接收当前四个允许话题，最多四个。
- 修正首页发布入口至国内发布页的相对路径，并在两个发布页展示「已带入话题」。
