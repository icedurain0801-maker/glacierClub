# v001 变更文档 · AI 陪伴机器人子项目4（知识图谱×RAG 第一阶段）

问答链路接入知识图谱:查询实体识别(别名表,最长优先匹配)→ 一跳关系事实注入 system prompt(截断优先级 persona>图谱事实>参考知识)→ refs 向后兼容扩展 `{type:'fact'}`。ingest 支持「别名」列自动抽取并落条目-实体关联表;B 端新增 `/api/kg` 实体列表+别名 CRUD,bots 增加 kg_enabled 开关。新增迁移 005_kg.sql(幂等)。KG 集成测试 12/12,回归 30/30 通过。
