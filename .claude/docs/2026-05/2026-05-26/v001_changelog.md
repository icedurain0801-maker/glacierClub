# v001 Changelog

- Updated overseas like interactions so `home-en.html` liked text and `+1` feedback use the primary blue state.
- Added blue `+1` popup feedback to overseas like buttons in feed, news, mood detail, post detail mini controls, and search results, while keeping the large `post-detail-en.html` like button without `+1`.
- Updated search results likes to use the same football icon replacement and pop animation as the other overseas like buttons.
- Ran `git diff --check`; only line-ending conversion warnings were reported.
