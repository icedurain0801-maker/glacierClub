# v003 Changelog · 2026-05-13

- bigPlayer overseas/home-en：首页 Tab 上方新增角色绑定 Banner（已绑定/未绑定双状态），含 Switch + Play 操作。
- bigPlayer overseas/home-en：新增 3 版本切换机制（网页版 / APP 版 / 简洁版），通过 `body[data-variant]` + 右上角浮动切换器控制，localStorage 持久化。默认网页版；APP 版显示角色绑定模块；简洁版通过 CSS 把热门推荐卡片改造为左图右文 + 左侧火条样式。
