# v008 Changelog

## Changes
- Moved the featured carousel top edge up in the web, miniprogram, and simple variants.
- Restored the full-width tab divider in those variants so it overlaps the active blue tab underline instead of sitting below it.

## Verification
- Playwright confirmed `#tab-nav` bottom, the active tab underline bottom, and `#featuredCarousel` top all share the same y-position in web, miniprogram, and simple variants.
