# v004 Changelog

## Changes
- Restored the `home-en.html` tab navigation bottom divider to match the app version.
- Kept the active tab underline offset so the blue underline overlaps the gray divider instead of appearing on a separate line.

## Verification
- Playwright measurement confirmed `#tab-nav` bottom and the active tab bottom are aligned at the same y-position.
