# v001 Changelog

## Summary

- Fixed the miniprogram variant top hero layout in `bigPlayer/client/overseas/home/home-en.html`.

## Details

- Added miniprogram-specific hero sizing so the hidden character bind strip no longer leaves the APP hero height active.
- Restored an earlier white fade for the miniprogram header, keeping tab labels readable over the hero background.
- Hid the topbar search button in miniprogram mode so it does not overlap the `Log in` CTA.
- Verified the miniprogram variant with Playwright.
