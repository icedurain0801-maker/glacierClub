# v001 — home-en 顶部加 hero 背景图

为 [home-en.html](../../../bigPlayer/client/overseas/home/home-en.html) 三个版本（网页/APP/简洁）顶部增加 hero 背景图，覆盖 status bar + Logo 顶栏 + Tab 栏（APP 版含角色绑定条），到 Tab 下沿渐变过渡到白底。Tab 文字保留默认深色保证可读，APP 版的角色绑定条改为半透明玻璃以融入背景。
