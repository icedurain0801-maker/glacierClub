## v004 上下文承接修复

- 日期: 2026-07-17
- 范围: C 端聊天上下文承接、英雄追问识别、前端会话隔离

### 本次修复

1. 后端补充英雄追问承接规则，支持这类自然追问继续继承上文英雄上下文:
   - `那台词呢`
   - `职业呢`
   - `阵营呢`
   - `稀有度呢`
   - `技能1呢`

2. 后端增加英雄字段直答分支:
   - 当用户追问英雄台词、阵营、职业、稀有度、头像时，优先直接从英雄卡结构化数据生成回答，不再掉回通用澄清。

3. 后端增强从 assistant 历史中恢复英雄主体的能力:
   - 优先从上一条 `herocard` payload 中提取英雄名
   - 兼容旧会话中已有英雄卡但用户后续只问短句追问的情况

4. 前端提升会话 schema 版本:
   - 强制新建本地 session key
   - 避免浏览器继续复用旧的脏上下文会话

### 验证结果

- 接口验证通过:
  - `介绍一下卡西迪` -> `那台词呢`
  - `介绍一下卡西迪` -> `职业呢`
- 旧会话验证通过:
  - `codex-context-800245be88e747adb65dfa5a48c3f83c` -> `那台词呢`
- `server/test/chatService.test.js` 通过

### 已知遗留

- `server/test/heroCardService.test.js` 仍有一个旧断言失败，与本次上下文承接修复无关:
  - 职业图标期望值: `/kb-images/1/69/1344_2.png`
  - 当前实际值: `/kb-images/1/69/1344_1.png`
