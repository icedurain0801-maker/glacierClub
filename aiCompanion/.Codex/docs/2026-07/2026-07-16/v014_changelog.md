# v014 Changelog

- 收紧角色问答兜底逻辑：当问题像是在查角色/实体，但没有命中完整角色资料时，不再放任模型编造英雄档案、稀有度、强度分级或“知识库未完全收录”一类模板话术。
- 新增知识库字面导出兜底：当 RAG 已搜到相关文本但不足以组成英雄卡时，直接返回命中的原始关键信息，避免脱离知识库自由发挥。
- `heroCardService` 对外暴露 `looksLikeHeroDetailQuery`，供聊天主链路复用角色查询判定。
- 补充 `chatService` 单测，覆盖未命中角色资料兜底和字面知识导出场景。
